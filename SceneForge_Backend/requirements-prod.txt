# FastAPI core dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
databases[sqlite]>=0.8.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
alembic>=1.11.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
aiofiles>=23.1.0

# Basic image processing (no ML models)
Pillow>=10.0.0
numpy>=1.24.0

# 3D Mesh Processing (required for output generation)
trimesh>=4.0.0
numpy-stl>=3.0.0

# Visualization
matplotlib>=3.7.0

# Background job processing (optional but recommended)
celery[redis]>=5.3.0
redis>=4.6.0

# TODO: Add ML packages later in separate requirements-ml.txt:
# - torch
# - torchvision
# - transformers
# - diffusers
# - accelerate
# - opencv-python-headless
